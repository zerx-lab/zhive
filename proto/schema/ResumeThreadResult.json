{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ResumeThreadResult",
  "description": "Result of the `engine/resume_thread` RPC.\n\n# Examples\n\n``` use std::sync::Arc; use zhive_proto::domain::ThreadId; use zhive_proto::rpc::ResumeThreadResult; let r = ResumeThreadResult::new(ThreadId(Arc::from(\"thread:native/abc\")), 10, 2); let v = serde_json::to_value(&r).unwrap(); assert_eq!(v[\"threadId\"], \"thread:native/abc\"); assert_eq!(v[\"itemsRestored\"], 10u32); assert_eq!(v[\"turnsRestored\"], 2u32); let back: ResumeThreadResult = serde_json::from_value(v).unwrap(); assert_eq!(back, r); ```",
  "type": "object",
  "required": [
    "itemsRestored",
    "threadId",
    "turnsRestored"
  ],
  "properties": {
    "itemsRestored": {
      "description": "Number of history items restored into memory.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    },
    "threadId": {
      "description": "Id of the resumed thread.",
      "type": "string"
    },
    "turnsRestored": {
      "description": "Number of turns spanned by the restored items.",
      "type": "integer",
      "format": "uint32",
      "minimum": 0.0
    }
  }
}