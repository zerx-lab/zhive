{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RenameThreadParams",
  "description": "Params of the `thread/rename` RPC.\n\nAn empty `name` clears the label (stored as `NULL` in the database). The operation is best-effort: rename is accepted and queued to the async persistence writer; `renamed: true` in the result means the request was enqueued, not that it has already hit disk.\n\n# Examples\n\n``` use std::sync::Arc; use zhive_proto::domain::ThreadId; use zhive_proto::rpc::RenameThreadParams; let p = RenameThreadParams::new( ThreadId(Arc::from(\"thread:native/abc\")), \"My feature branch\".into(), ); let v = serde_json::to_value(&p).unwrap(); assert_eq!(v[\"name\"], \"My feature branch\"); let back: RenameThreadParams = serde_json::from_value(v).unwrap(); assert_eq!(back, p); ```",
  "type": "object",
  "required": [
    "name",
    "threadId"
  ],
  "properties": {
    "name": {
      "description": "New display name; an empty string clears the name (stored as `NULL`).",
      "type": "string"
    },
    "threadId": {
      "description": "Thread to rename.",
      "type": "string"
    }
  }
}